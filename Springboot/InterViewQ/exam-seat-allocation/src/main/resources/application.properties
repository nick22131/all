spring.application.name=Exam Seat Allocation Engine
# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/exam_allocation_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=1515

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Server Configuration
server.port=8081

# Logging
logging.level.com.exam.allocation=DEBUG